# Private Task Demo

This workspace is the standalone SDK example for private-task payload construction.

Run it from the repo root:

```bash
npm run demo:private-task -- --help
```

Or directly via the workspace:

```bash
npm run start --workspace=@tetsuo-ai/private-task-demo -- --help
```
