## Tech Debt Report - 2026-04-02

### Critical (Fix Now)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in the touched surface | n/a | n/a | n/a |

### High (Fix This Sprint)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in the touched surface | n/a | n/a | n/a |

### Medium (Backlog)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| Scene lifecycle logic is duplicated between sequential and simultaneous engines | `concordia_bridge/instrumented_engine.py`, `concordia_bridge/instrumented_simultaneous_engine.py` | Future scene behavior changes can drift between engine modes and reintroduce launch/runtime regressions | Extract shared scene-state handling into a helper on the base engine or a small scene coordinator object |
| Concordia bridge still logs `send() missing request_id` during live runs | daemon log only; likely in `agenc-plugin-concordia/src/adapter.ts` send path | Non-blocking right now, but it indicates incomplete request correlation for Concordia turns and could affect observability or response routing later | Trace the Concordia request/response handoff and enforce request ID propagation for simulation turns |

### Duplications Found
| Pattern | Locations | Lines | Refactor To |
|---------|-----------|-------|-------------|
| Scene initialization and transition bookkeeping | `concordia_bridge/instrumented_engine.py`, `concordia_bridge/instrumented_simultaneous_engine.py` | `instrumented_simultaneous_engine.py:77-116` plus the equivalent block in the sequential engine | Shared helper on `InstrumentedSequentialEngine` |

### Summary
- Total issues: 2
- Estimated cleanup: 3 files
- Recommended priority: unify scene handling across engine modes before adding more scene features
