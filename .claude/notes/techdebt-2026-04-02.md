## Tech Debt Report - 2026-04-02

### Critical (Fix Now)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in the touched surface | n/a | n/a | n/a |

### High (Fix This Sprint)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in the touched surface | n/a | n/a | n/a |

### Medium (Backlog)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| Scene lifecycle logic is duplicated between sequential and simultaneous engines | `concordia_bridge/instrumented_engine.py`, `concordia_bridge/instrumented_simultaneous_engine.py` | Future scene behavior changes can drift between engine modes and reintroduce launch/runtime regressions | Extract shared scene-state handling into a helper on the base engine or a small scene coordinator object |
| Concordia bridge still logs `send() missing request_id` during live runs | daemon log only; likely in `agenc-plugin-concordia/src/adapter.ts` send path | Non-blocking right now, but it indicates incomplete request correlation for Concordia turns and could affect observability or response routing later | Trace the Concordia request/response handoff and enforce request ID propagation for simulation turns |

### Duplications Found
| Pattern | Locations | Lines | Refactor To |
|---------|-----------|-------|-------------|
| Scene initialization and transition bookkeeping | `concordia_bridge/instrumented_engine.py`, `concordia_bridge/instrumented_simultaneous_engine.py` | `instrumented_simultaneous_engine.py:77-116` plus the equivalent block in the sequential engine | Shared helper on `InstrumentedSequentialEngine` |

### Summary
- Total issues: 2
- Estimated cleanup: 3 files
- Recommended priority: unify scene handling across engine modes before adding more scene features

## Tech Debt Report - 2026-04-02 (Phase 0 Concordia Lifecycle)

### Critical (Fix Now)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in the Phase 0 touched surface | n/a | n/a | n/a |

### High (Fix This Sprint)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in the Phase 0 touched surface | n/a | n/a | n/a |

### Medium (Backlog)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| Pending-response dispatch failure cleanup is duplicated between `/act` and `/generate-agents` | `agenc-plugin-concordia/src/adapter.ts` | Future request lifecycle changes can fix one path and miss the other, reintroducing hanging promises or inconsistent logging | Extract a shared dispatch helper that owns create, log, dispatch, reject-on-failure, and drain semantics |
| Sequential and simultaneous engine stop/step bookkeeping still lives in two long `run_loop(...)` bodies | `concordia_bridge/instrumented_engine.py`, `concordia_bridge/instrumented_simultaneous_engine.py` | The Phase 0 stop checks are now correct, but future lifecycle changes can drift across modes and reopen freeze bugs | Pull shared stop-gate and step-accounting helpers up into a smaller common coordinator layer |
| `ProxyEntity._perform_act_request(...)` still mixes request construction, HTTP transport, and response parsing in one large method | `concordia_bridge/proxy_entity.py` | The new cancellation path is correct, but the method remains harder to test and evolve than it should be | Split transport, payload build, and response parse into smaller helpers once Phase 1 work starts |

### Duplications Found
| Pattern | Locations | Lines | Refactor To |
|---------|-----------|-------|-------------|
| Dispatch-then-cleanup request lifecycle | `agenc-plugin-concordia/src/adapter.ts` `/act` and `/generate-agents` paths | near `handleAct(...)` and `handleGenerateAgents(...)` | Shared pending-response dispatch helper |
| Stop-gate and step-accounting flow | `concordia_bridge/instrumented_engine.py`, `concordia_bridge/instrumented_simultaneous_engine.py` | `run_loop(...)` bodies | Shared lifecycle coordinator/helper on the engine base |

### Summary
- Total issues: 3
- Estimated cleanup: 4 files
- Recommended priority: extract the duplicated adapter dispatch lifecycle before Phase 1 adds more simulation request types


## Tech Debt Report - 2026-04-02 (Phase 1 Simulation Identity)

### Critical (Fix Now)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in the Phase 1 touched surface | n/a | n/a | n/a |

### High (Fix This Sprint)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in the Phase 1 touched surface | n/a | n/a | n/a |

### Medium (Backlog)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| Run-identity payload assembly is duplicated across the bridge and runner layers | `agenc-plugin-concordia/src/adapter.ts`, `agenc-plugin-concordia/src/bridge-http.ts`, `concordia_bridge/runner.py`, `concordia_bridge/proxy_entity.py`, `concordia_bridge/checkpoint.py` | Future identity changes can easily update one transport path and miss another, reopening cross-run ambiguity | Extract shared identity helpers/builders per language before Phase 2 adds registry and replay endpoints |
| `SessionManager.get(agentId)` remains a simulation-ambiguous accessor | `agenc-plugin-concordia/src/session-manager.ts` | It is safe today only because Phase 1 avoids it in critical paths; reusing it later can bypass simulation scoping by accident | Remove it in Phase 2 or replace it with a simulation-aware lookup API |
| Concordia adapter event routing still concentrates a lot of behavior in long methods | `agenc-plugin-concordia/src/adapter.ts` (`send`, `handleEvent`) | The new simulation identity checks are correct, but future lifecycle/isolation changes are more error-prone in these larger methods | Split request-correlation, active-sim validation, and periodic-task orchestration into smaller helpers |

### Duplications Found
| Pattern | Locations | Lines | Refactor To |
|---------|-----------|-------|-------------|
| Repeated `simulation_id` / `lineage_id` / `parent_simulation_id` payload construction | `agenc-plugin-concordia/src/adapter.ts`, `agenc-plugin-concordia/src/bridge-http.ts`, `concordia_bridge/runner.py`, `concordia_bridge/proxy_entity.py`, `concordia_bridge/checkpoint.py` | multiple request/event/checkpoint payload sites | Shared identity payload builder/helper per runtime layer |
| Long event/request handling methods | `agenc-plugin-concordia/src/adapter.ts` | `send` at ~63 lines, `handleEvent` at ~112 lines | Smaller helpers for correlation, active-run guards, and periodic side effects |

### Summary
- Total issues: 3
- Estimated cleanup: 6 files
- Recommended priority: centralize run-identity payload creation before Phase 2 introduces simulation registries and replay APIs
