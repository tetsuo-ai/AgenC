## Tech Debt Report - 2026-04-02

### Critical (Fix Now)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in the touched Concordia/runtime path after this audit pass. | n/a | n/a | n/a |

### High (Fix This Sprint)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None currently outstanding in the touched Concordia packaging path. | n/a | n/a | n/a |

### Medium (Backlog)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| Concordia adapter remains oversized | `agenc-plugin-concordia/src/adapter.ts` | Launch/setup/resume/act/observe/event/state logic stays tightly coupled, which makes regressions easier to reintroduce. | Split into focused modules for HTTP orchestration, simulation lifecycle, and memory/event handling. |
| Concordia memory wiring remains oversized | `agenc-plugin-concordia/src/memory-wiring.ts` | Identity, semantic retrieval, graph updates, social memory, and state shaping are all in one file, making it hard to validate changes in isolation. | Split into prompt-context, social/world-memory, and graph/activation submodules. |
| Chat executor remains very large even though Concordia contract handling is now correct | `agenc-core/runtime/src/llm/chat-executor.ts` | Future contract-specific fixes are harder to reason about in a 3k+ line file. | Continue extracting contract-specific or context-injection logic into focused helpers. |

### Duplications Found
| Pattern | Locations | Lines | Refactor To |
|---------|-----------|-------|-------------|
| Concordia message persistence helpers were duplicated and are now centralized | `runtime/src/gateway/channel-wiring.ts`, `runtime/src/gateway/daemon-text-channel-turn.ts` | resolved in this pass | Keep using `runtime/src/gateway/channel-message-memory.ts` for future Concordia/session-scoping helpers. |

### Summary
- Total issues: 4
- Estimated cleanup: 4 files
- Recommended priority: fix the `simulation-runner.ts` dual-format path resolution warning before the next packaging/release pass
