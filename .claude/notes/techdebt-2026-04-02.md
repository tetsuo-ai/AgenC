## Tech Debt Report - 2026-04-02

### Critical (Fix Now)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found. | n/a | n/a | n/a |

### High (Fix This Sprint)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| Long function (145 lines) | agenc-plugin-concordia/src/bridge-http.ts:122 | Large functions are harder to review, test, and modify safely. | Split the function into smaller helpers around validation, state mutation, and side effects. |
| Long function (106 lines) | agenc-plugin-concordia/src/memory-wiring.ts:1199 | Large functions are harder to review, test, and modify safely. | Split the function into smaller helpers around validation, state mutation, and side effects. |
| Long function (155 lines) | concordia_bridge/demo.py:235 | Large functions make it easier to miss edge cases. | Split the function into focused helpers and isolate error handling. |
| Long function (110 lines) | examples/helius-webhook/index.ts:397 | Large functions are harder to review, test, and modify safely. | Split the function into smaller helpers around validation, state mutation, and side effects. |
| Long function (297 lines) | scripts/techdebt.mjs:162 | Large functions are harder to review, test, and modify safely. | Split the function into smaller helpers around validation, state mutation, and side effects. |

### Medium (Backlog)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| Long function (78 lines) | agenc-plugin-concordia/src/bridge-http.ts:272 | Large functions are harder to review, test, and modify safely. | Split the function into smaller helpers around validation, state mutation, and side effects. |
| Long function (51 lines) | agenc-plugin-concordia/src/bridge-http.ts:351 | Large functions are harder to review, test, and modify safely. | Split the function into smaller helpers around validation, state mutation, and side effects. |
| Long function (52 lines) | agenc-plugin-concordia/src/bridge-http.ts:541 | Large functions are harder to review, test, and modify safely. | Split the function into smaller helpers around validation, state mutation, and side effects. |
| Repeated numeric literal 200 | agenc-plugin-concordia/src/bridge-http.ts:135 | Repeated literals drift over time when not centralized. | Promote the value to a named constant if it represents a stable protocol or business rule. |
| Repeated numeric literal 404 | agenc-plugin-concordia/src/bridge-http.ts:190 | Repeated literals drift over time when not centralized. | Promote the value to a named constant if it represents a stable protocol or business rule. |
| Repeated numeric literal 65535 | agenc-plugin-concordia/src/index.ts:48 | Repeated literals drift over time when not centralized. | Promote the value to a named constant if it represents a stable protocol or business rule. |
| Long function (54 lines) | agenc-plugin-concordia/src/memory-wiring.ts:542 | Large functions are harder to review, test, and modify safely. | Split the function into smaller helpers around validation, state mutation, and side effects. |
| Long function (76 lines) | agenc-plugin-concordia/src/memory-wiring.ts:674 | Large functions are harder to review, test, and modify safely. | Split the function into smaller helpers around validation, state mutation, and side effects. |
| Long function (56 lines) | agenc-plugin-concordia/src/memory-wiring.ts:927 | Large functions are harder to review, test, and modify safely. | Split the function into smaller helpers around validation, state mutation, and side effects. |
| Nested control flow (depth 4) | agenc-plugin-concordia/src/memory-wiring.ts:1009 | Nested branches increase cognitive load during review. | Flatten the control flow with early returns or smaller helper functions. |
| Long function (73 lines) | agenc-plugin-concordia/src/simulation-runner.ts:15 | Large functions are harder to review, test, and modify safely. | Split the function into smaller helpers around validation, state mutation, and side effects. |
| Repeated numeric literal 127 | agenc-plugin-concordia/tests/bridge-http.test.ts:162 | Repeated literals drift over time when not centralized. | Promote the value to a named constant if it represents a stable protocol or business rule. |

### Duplications Found
| Pattern | Locations | Lines | Refactor To |
|---------|-----------|-------|-------------|
| Exact duplicate window (10 normalized lines) | examples/risc0-proof-demo/index.ts:25, examples/simple-usage/index.ts:25 | 10 lines | Extract a shared helper or reduce the repeated validation/state-update block. |
| Exact duplicate window (10 normalized lines) | examples/risc0-proof-demo/index.ts:26, examples/simple-usage/index.ts:26 | 10 lines | Extract a shared helper or reduce the repeated validation/state-update block. |
| Exact duplicate window (10 normalized lines) | examples/risc0-proof-demo/index.ts:27, examples/simple-usage/index.ts:27 | 10 lines | Extract a shared helper or reduce the repeated validation/state-update block. |
| Exact duplicate window (10 normalized lines) | examples/risc0-proof-demo/index.ts:28, examples/simple-usage/index.ts:28 | 10 lines | Extract a shared helper or reduce the repeated validation/state-update block. |
| Exact duplicate window (10 normalized lines) | examples/risc0-proof-demo/index.ts:29, examples/simple-usage/index.ts:29 | 10 lines | Extract a shared helper or reduce the repeated validation/state-update block. |
| Exact duplicate window (10 normalized lines) | examples/risc0-proof-demo/index.ts:30, examples/simple-usage/index.ts:30 | 10 lines | Extract a shared helper or reduce the repeated validation/state-update block. |
| Exact duplicate window (10 normalized lines) | examples/risc0-proof-demo/index.ts:31, examples/simple-usage/index.ts:31 | 10 lines | Extract a shared helper or reduce the repeated validation/state-update block. |

### Summary
- Total issues: 43
- Estimated cleanup: 21 files
- Recommended priority: Long function (145 lines)

