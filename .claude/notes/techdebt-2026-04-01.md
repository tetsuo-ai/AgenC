## Tech Debt Report - 2026-04-01

### Critical (Fix Now)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found. | n/a | n/a | n/a |

### High (Fix This Sprint)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| Deep nesting (depth 5) | agenc-plugin-concordia/src/adapter.ts:121 | Heavily nested control flow is error-prone and obscures failure paths. | Introduce guard clauses or extract nested branches into helpers. |
| Deep nesting (depth 5) | agenc-plugin-concordia/src/adapter.ts:122 | Heavily nested control flow is error-prone and obscures failure paths. | Introduce guard clauses or extract nested branches into helpers. |
| Deep nesting (depth 5) | agenc-plugin-concordia/src/adapter.ts:123 | Heavily nested control flow is error-prone and obscures failure paths. | Introduce guard clauses or extract nested branches into helpers. |
| Deep nesting (depth 5) | agenc-plugin-concordia/src/adapter.ts:124 | Heavily nested control flow is error-prone and obscures failure paths. | Introduce guard clauses or extract nested branches into helpers. |
| Deep nesting (depth 5) | agenc-plugin-concordia/src/adapter.ts:125 | Heavily nested control flow is error-prone and obscures failure paths. | Introduce guard clauses or extract nested branches into helpers. |
| Deep nesting (depth 5) | agenc-plugin-concordia/src/adapter.ts:126 | Heavily nested control flow is error-prone and obscures failure paths. | Introduce guard clauses or extract nested branches into helpers. |
| Deep nesting (depth 5) | agenc-plugin-concordia/src/adapter.ts:136 | Heavily nested control flow is error-prone and obscures failure paths. | Introduce guard clauses or extract nested branches into helpers. |
| Deep nesting (depth 5) | agenc-plugin-concordia/src/adapter.ts:137 | Heavily nested control flow is error-prone and obscures failure paths. | Introduce guard clauses or extract nested branches into helpers. |
| Deep nesting (depth 5) | agenc-plugin-concordia/src/adapter.ts:138 | Heavily nested control flow is error-prone and obscures failure paths. | Introduce guard clauses or extract nested branches into helpers. |
| Deep nesting (depth 5) | agenc-plugin-concordia/src/adapter.ts:139 | Heavily nested control flow is error-prone and obscures failure paths. | Introduce guard clauses or extract nested branches into helpers. |
| Deep nesting (depth 5) | agenc-plugin-concordia/src/adapter.ts:140 | Heavily nested control flow is error-prone and obscures failure paths. | Introduce guard clauses or extract nested branches into helpers. |
| Deep nesting (depth 5) | agenc-plugin-concordia/src/adapter.ts:141 | Heavily nested control flow is error-prone and obscures failure paths. | Introduce guard clauses or extract nested branches into helpers. |

### Medium (Backlog)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| Nested control flow (depth 4) | agenc-plugin-concordia/src/adapter.ts:103 | Nested branches increase cognitive load during review. | Flatten the control flow with early returns or smaller helper functions. |
| Nested control flow (depth 4) | agenc-plugin-concordia/src/adapter.ts:104 | Nested branches increase cognitive load during review. | Flatten the control flow with early returns or smaller helper functions. |
| Nested control flow (depth 4) | agenc-plugin-concordia/src/adapter.ts:105 | Nested branches increase cognitive load during review. | Flatten the control flow with early returns or smaller helper functions. |
| Nested control flow (depth 4) | agenc-plugin-concordia/src/adapter.ts:106 | Nested branches increase cognitive load during review. | Flatten the control flow with early returns or smaller helper functions. |
| Nested control flow (depth 4) | agenc-plugin-concordia/src/adapter.ts:107 | Nested branches increase cognitive load during review. | Flatten the control flow with early returns or smaller helper functions. |
| Nested control flow (depth 4) | agenc-plugin-concordia/src/adapter.ts:108 | Nested branches increase cognitive load during review. | Flatten the control flow with early returns or smaller helper functions. |
| Nested control flow (depth 4) | agenc-plugin-concordia/src/adapter.ts:109 | Nested branches increase cognitive load during review. | Flatten the control flow with early returns or smaller helper functions. |
| Nested control flow (depth 4) | agenc-plugin-concordia/src/adapter.ts:110 | Nested branches increase cognitive load during review. | Flatten the control flow with early returns or smaller helper functions. |
| Nested control flow (depth 4) | agenc-plugin-concordia/src/adapter.ts:111 | Nested branches increase cognitive load during review. | Flatten the control flow with early returns or smaller helper functions. |
| Nested control flow (depth 4) | agenc-plugin-concordia/src/adapter.ts:112 | Nested branches increase cognitive load during review. | Flatten the control flow with early returns or smaller helper functions. |
| Nested control flow (depth 4) | agenc-plugin-concordia/src/adapter.ts:113 | Nested branches increase cognitive load during review. | Flatten the control flow with early returns or smaller helper functions. |
| Nested control flow (depth 4) | agenc-plugin-concordia/src/adapter.ts:114 | Nested branches increase cognitive load during review. | Flatten the control flow with early returns or smaller helper functions. |

### Duplications Found
| Pattern | Locations | Lines | Refactor To |
|---------|-----------|-------|-------------|
| Exact duplicate window (10 normalized lines) | concordia_bridge/instrumented_engine.py:161, concordia_bridge/instrumented_simultaneous_engine.py:47 | 10 lines | Extract a shared helper or reduce the repeated validation/state-update block. |
| Exact duplicate window (10 normalized lines) | concordia_bridge/instrumented_engine.py:162, concordia_bridge/instrumented_simultaneous_engine.py:48 | 10 lines | Extract a shared helper or reduce the repeated validation/state-update block. |
| Exact duplicate window (10 normalized lines) | examples/risc0-proof-demo/index.ts:25, examples/simple-usage/index.ts:25 | 10 lines | Extract a shared helper or reduce the repeated validation/state-update block. |
| Exact duplicate window (10 normalized lines) | examples/risc0-proof-demo/index.ts:26, examples/simple-usage/index.ts:26 | 10 lines | Extract a shared helper or reduce the repeated validation/state-update block. |
| Exact duplicate window (10 normalized lines) | examples/risc0-proof-demo/index.ts:27, examples/simple-usage/index.ts:27 | 10 lines | Extract a shared helper or reduce the repeated validation/state-update block. |
| Exact duplicate window (10 normalized lines) | examples/risc0-proof-demo/index.ts:28, examples/simple-usage/index.ts:28 | 10 lines | Extract a shared helper or reduce the repeated validation/state-update block. |
| Exact duplicate window (10 normalized lines) | examples/risc0-proof-demo/index.ts:29, examples/simple-usage/index.ts:29 | 10 lines | Extract a shared helper or reduce the repeated validation/state-update block. |
| Exact duplicate window (10 normalized lines) | examples/risc0-proof-demo/index.ts:30, examples/simple-usage/index.ts:30 | 10 lines | Extract a shared helper or reduce the repeated validation/state-update block. |
| Exact duplicate window (10 normalized lines) | examples/risc0-proof-demo/index.ts:31, examples/simple-usage/index.ts:31 | 10 lines | Extract a shared helper or reduce the repeated validation/state-update block. |

### Summary
- Total issues: 1564
- Estimated cleanup: 27 files
- Recommended priority: Deep nesting (depth 5)

## Addendum - Concordia runtime cleanup (2026-04-01)

### Critical (Fix Now)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None in the Concordia dashboard/turn-path fix set | N/A | Live `agenc ui` and Concordia browser launch are healthy after rebuild/redeploy | Keep verification on the installed runtime path, not just local source builds |

### High (Fix This Sprint)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| `adapter.ts` is still carrying too much session/orchestration logic | `agenc-plugin-concordia/src/adapter.ts` | Concordia launch/setup/observe/act lifecycle changes remain expensive to reason about | Split launch/session/state orchestration into smaller helpers or modules |
| Runner stream logging still depends on log-line parsing heuristics | `agenc-plugin-concordia/src/simulation-runner.ts` | Non-standard stderr/stdout formats can still be misclassified | Move to structured side-channel status or explicit stderr discipline in the Python runner |

### Medium (Backlog)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| Concordia fallback embeddings are deterministic and stable, but still lower fidelity than real sentence embeddings | `concordia_bridge/runner.py` | Memory quality is acceptable for smoke/dev flows but weaker than a proper embedding stack | Install/support `sentence-transformers` in the Concordia environment for higher-quality GM memory retrieval |
| Legacy phase references to `CONCORDIA_TODO.MD` remain in module docstrings | `concordia_bridge/*.py`, `agenc-plugin-concordia/src/*.ts` | The implementation works, but the wording keeps the code feeling transitional | Replace phase-era references with current ownership/runtime docs |

## Addendum - Concordia plan restoration (2026-04-01)

### Critical (Fix Now)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None in the restored plan-contract patch set | N/A | Deterministic session IDs, request-correlated routing, semantic memory wiring, and checkpoint/resume surfaces are now aligned with the intended Concordia architecture | Keep the new request-id and checkpoint contracts stable across future bridge changes |

### High (Fix This Sprint)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| `adapter.ts` still owns too many Concordia lifecycle concerns even after restoring the missing plan seams | `agenc-plugin-concordia/src/adapter.ts` | Setup, act, checkpoint, resume, runner, and response routing changes still fan into one file | Split adapter responsibilities into dedicated session, checkpoint, and runner orchestration modules |
| Engine state restoration logic is duplicated between sequential and simultaneous engines | `concordia_bridge/instrumented_engine.py`, `concordia_bridge/instrumented_simultaneous_engine.py` | Resume/step semantics can drift again if only one engine path changes | Extract a shared restoration helper or shared state contract module for both engines |

### Medium (Backlog)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| The bridge compatibility shim exists only to preserve the historic `event_stream.py` import path from the original plan | `concordia_bridge/event_stream.py` | It keeps compatibility, but it is easy for future maintainers to mistake it for an actively owned event-stream implementation | Document the shim role explicitly in current Concordia architecture notes |
| Checkpoint payloads now include entity state snapshots, but payload versioning is still implicit | `concordia_bridge/checkpoint.py` | Future schema changes will be harder to evolve safely without an explicit checkpoint version contract | Add a checkpoint schema/version field before the next checkpoint format expansion |
