## Tech Debt Report - 2026-03-09

### Scope
- Focused end-of-session scan for the Live TUI surfaces touched in this pass:
  - `scripts/agenc-watch.mjs`
  - `scripts/agenc-trace-watch.mjs`
- Detailed watch/TUI findings remain in `.claude/notes/techdebt-2026-03-09-agenc-watch-tui.md`

### Critical (Fix Now)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in touched files | - | - | - |

### High (Fix This Sprint)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in touched files | - | - | - |

### Medium (Backlog)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| Live and trace panes still duplicate terminal-theme primitives | `scripts/agenc-watch.mjs`, `scripts/agenc-trace-watch.mjs` | Aesthetic changes can drift again because palette, badge, and frame logic are edited in two places | Extract a shared `terminal-theme` helper before the next major TUI pass |
| Live watch remains a large combined transport/state/render script | `scripts/agenc-watch.mjs` | Future visual or behavioral changes still carry high regression risk | Split render/layout from websocket/event/input plumbing |

### Duplications Found
| Pattern | Locations | Lines | Refactor To |
|---------|-----------|-------|-------------|
| Terminal palette and chrome helpers repeated across live and trace watchers | `scripts/agenc-watch.mjs`, `scripts/agenc-trace-watch.mjs` | ~35 | Shared terminal theme module |

### Summary
- Total issues: 2
- Estimated cleanup: 2 files
- Recommended priority: extract shared terminal-theme primitives, then split the live watch transport/render boundary

---

## Runtime Addendum - 2026-03-09

### Scope
- Focused end-of-session scan for the runtime continuity and compaction surfaces touched in this pass:
  - `runtime/src/gateway/daemon.ts`
  - `runtime/src/gateway/delegation-tool.ts`
  - `runtime/src/gateway/sub-agent.ts`
  - `runtime/src/gateway/tool-handler-factory.ts`
  - `runtime/src/llm/chat-executor.ts`
  - `runtime/src/llm/chat-executor-text.ts`
  - `runtime/src/llm/grok/adapter.ts`
  - `runtime/src/memory/ingestion.ts`

### Critical (Fix Now)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in touched files | - | - | - |

### High (Fix This Sprint)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in touched files | - | - | - |

### Medium (Backlog)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| Core continuity path still depends on very large orchestration files | `runtime/src/gateway/daemon.ts`, `runtime/src/llm/chat-executor.ts`, `runtime/src/llm/grok/adapter.ts`, `runtime/src/llm/chat-executor-text.ts` | Stateful replay and memory regressions remain expensive to reason about and easy to re-break because flow control, transport, and policy logic are concentrated in a few hotspots | Continue decomposition into smaller continuity/transport/memory modules before the next major autonomy pass |
| Compaction ingestion policy is still encoded inline with broader memory ingestion behavior | `runtime/src/memory/ingestion.ts` | Future hook changes can reintroduce false-positive warnings or mixed semantics between generated and non-generated summaries | Extract explicit compact-event validation helpers and cover manual/local compaction payload shapes directly |

### Duplications Found
| Pattern | Locations | Lines | Refactor To |
|---------|-----------|-------|-------------|
| Stateful continuation and routing logic still spans daemon, executor, and adapter layers with repeated diagnostics shaping | `runtime/src/gateway/daemon.ts`, `runtime/src/llm/chat-executor.ts`, `runtime/src/llm/grok/adapter.ts` | ~60-90 per layer | Shared continuity diagnostics and request-shaping helpers |

### Summary
- Total issues: 2 medium
- Estimated cleanup: 4 files
- Recommended priority: continue decomposing the continuity path, then isolate compaction hook validation

---

## Runtime Endurance Addendum - 2026-03-09

### Scope
- Focused scan for the additional endurance-memory stabilization work validated from `~/.agenc/daemon.log` in replay runs `R10` and `R11`:
  - `runtime/src/channels/webchat/plugin.ts`
  - `runtime/src/gateway/sub-agent.ts`
  - `runtime/src/gateway/tool-handler-factory.ts`
  - `runtime/src/llm/chat-executor.ts`
  - `runtime/src/llm/chat-executor-planner.ts`
  - `runtime/src/llm/chat-executor-text.ts`
  - `scripts/agenc-watch.mjs`

### Critical (Fix Now)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in touched files | - | - | - |

### High (Fix This Sprint)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in touched files | - | - | - |

### Medium (Backlog)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| Planner output contract is still provider-fragile even after salvage support | `runtime/src/llm/chat-executor-planner.ts`, `runtime/src/llm/chat-executor.ts` | The runtime now recovers from direct planner tool calls, but future planner drift can still force more compatibility code into the executor path | Isolate planner-response normalization behind a dedicated adapter and expand replay fixtures for malformed planner output shapes |
| Delegated exact-response enforcement is split across session reuse, prompt sanitization, and text reconciliation layers | `runtime/src/gateway/tool-handler-factory.ts`, `runtime/src/llm/chat-executor-text.ts`, `runtime/src/gateway/sub-agent.ts` | Parent/child memory endurance remains correct, but future changes can re-break store vs recall handling or literal restoration in subtle ways | Consolidate delegated exact-response contract handling into a shared helper with store/recall classification tests |

### Duplications Found
| Pattern | Locations | Lines | Refactor To |
|---------|-----------|-------|-------------|
| Delegated-memory contract shaping is repeated across delegation and text-reconciliation layers | `runtime/src/gateway/tool-handler-factory.ts`, `runtime/src/llm/chat-executor-text.ts` | ~40-60 | Shared exact-contract/delegation normalization helper |

### Summary
- Total issues: 2 medium
- Estimated cleanup: 5 files
- Recommended priority: isolate planner-response normalization, then consolidate delegated exact-response handling

---

## Runtime Child-Memory Addendum - 2026-03-09

### Scope
- Focused scan for the delegated child-memory contract fixes validated from `~/.agenc/daemon.log` during live replay `MEMCORE-20260309-1302`:
  - `runtime/src/gateway/tool-handler-factory.ts`
  - `runtime/src/utils/delegation-validation.ts`
  - `runtime/src/llm/chat-executor-planner.ts`
  - `runtime/src/llm/chat-executor.test.ts`

### Critical (Fix Now)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in touched files | - | - | - |

### High (Fix This Sprint)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in touched files | - | - | - |

### Medium (Backlog)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| Planner salvage is still the normal path for delegated memory turns instead of strict planner JSON | `runtime/src/llm/chat-executor-planner.ts`, `runtime/src/llm/chat-executor.ts` | Memory continuity now works, but planner-shape drift is still masked by compatibility logic and can hide future regressions behind `planner_tool_call_salvaged` noise | Add a dedicated planner-output normalization layer and explicit replay fixtures for function-call-first planner responses |
| Delegated contract normalization remains split between tool dispatch and validation | `runtime/src/gateway/tool-handler-factory.ts`, `runtime/src/utils/delegation-validation.ts` | Store/recall fixes are correct today, but future changes can drift literal rewriting, placeholder matching, and acceptance enforcement back out of sync | Extract a shared delegated-contract normalization module used by both dispatch and validation |

### Duplications Found
| Pattern | Locations | Lines | Refactor To |
|---------|-----------|-------|-------------|
| Delegated exact-output contract shaping is duplicated between tool dispatch rewriting and validator-side placeholder matching | `runtime/src/gateway/tool-handler-factory.ts`, `runtime/src/utils/delegation-validation.ts` | ~45-70 | Shared delegated-contract normalization helper |

### Summary
- Total issues: 2 medium
- Estimated cleanup: 4 files
- Recommended priority: isolate planner normalization, then unify delegated contract shaping across dispatch and validation

---

## Runtime Medium-Debt Follow-up - 2026-03-09

### Scope
- Focused post-fix scan for the delegated memory/planner surfaces changed in this follow-up:
  - `runtime/src/gateway/tool-handler-factory.ts`
  - `runtime/src/utils/delegation-validation.ts`
  - `runtime/src/utils/delegated-contract-normalization.ts`
  - `runtime/src/llm/chat-executor.ts`
  - `runtime/src/llm/chat-executor-planner-normalization.ts`
  - `runtime/src/llm/chat-executor-verifier.ts`
  - `runtime/src/llm/chat-executor.test.ts`

### Critical (Fix Now)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in touched files | - | - | - |

### High (Fix This Sprint)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in touched files | - | - | - |

### Medium (Backlog)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| Chat executor continuity path is still concentrated in a very large orchestrator | `runtime/src/llm/chat-executor.ts` | Planner normalization is cleaner, but long-term autonomy work still has to thread through a 3.3k-line executor | Continue extracting verifier/continuity and tool-loop policy seams into smaller modules before the next major autonomy pass |
| Delegation entrypoint still mixes approval/routing/runtime orchestration after the contract extraction | `runtime/src/gateway/tool-handler-factory.ts` | Delegated contract drift is reduced, but future child-session policy changes still land in a 1.3k-line handler surface | Split delegation execution into its own module once the next child-session/runtime pass begins |

### Duplications Found
| Pattern | Locations | Lines | Refactor To |
|---------|-----------|-------|-------------|
| Resolved: delegated exact-output parsing and recall/store prompt shaping are no longer duplicated between dispatch and validation | `runtime/src/gateway/tool-handler-factory.ts`, `runtime/src/utils/delegation-validation.ts` | ~120 removed from old locations | `runtime/src/utils/delegated-contract-normalization.ts` |
| Resolved: planner parse-plus-salvage merging is no longer hand-built in the executor | `runtime/src/llm/chat-executor.ts`, `runtime/src/llm/chat-executor-planner.ts` | ~15 executor lines removed | `runtime/src/llm/chat-executor-planner-normalization.ts` |

### Summary
- Total issues: 2 medium
- Estimated cleanup: 2 large files
- Recommended priority: keep decomposing `ChatExecutor`, then peel delegated runtime orchestration out of `tool-handler-factory`

---

## Runtime Structural Decomposition Follow-up - 2026-03-09

### Scope
- Focused post-refactor scan for the remaining large runtime orchestration surfaces:
  - `runtime/src/gateway/tool-handler-factory.ts`
  - `runtime/src/gateway/tool-handler-factory-delegation.ts`
  - `runtime/src/llm/chat-executor.ts`
  - `runtime/src/llm/chat-executor-planner-verifier-loop.ts`

### Critical (Fix Now)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in touched files | - | - | - |

### High (Fix This Sprint)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in touched files | - | - | - |

### Medium (Backlog)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| Daemon still remains the dominant runtime incident surface | `runtime/src/gateway/daemon.ts` | Even after ChatExecutor and delegation extractions, most cross-cutting runtime orchestration is still concentrated in the daemon | Split channel/webchat orchestration and background-run control before the next autonomy-hardening pass |
| ChatExecutor is smaller but still large enough to slow continuity changes | `runtime/src/llm/chat-executor.ts` | Planner normalization and verifier loop are extracted, but provider transport, tool loop, and evaluation still share one file | Extract provider fallback/transport or evaluator flow next |

### Duplications Found
| Pattern | Locations | Lines | Refactor To |
|---------|-----------|-------|-------------|
| Resolved: delegated execute-with-agent orchestration is no longer mixed into the generic session tool handler | `runtime/src/gateway/tool-handler-factory.ts` | ~550 lines moved | `runtime/src/gateway/tool-handler-factory-delegation.ts` |
| Resolved: planner pipeline retry and verifier-round logic is no longer inline in the main executor | `runtime/src/llm/chat-executor.ts` | ~150 lines moved | `runtime/src/llm/chat-executor-planner-verifier-loop.ts` |

### Summary
- Total issues: 2 medium
- Estimated cleanup: 2 large files
- Recommended priority: decompose the daemon, then extract ChatExecutor provider transport

---

## Runtime Daemon Stateful-Session Decomposition - 2026-03-09

### Scope
- Focused post-refactor scan for the daemon continuity surfaces changed in this pass:
  - `runtime/src/gateway/daemon.ts`
  - `runtime/src/gateway/daemon-session-state.ts`
  - `runtime/src/gateway/daemon-session-state.test.ts`
  - `runtime/src/llm/chat-executor.ts`

### Critical (Fix Now)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in touched files | - | - | - |

### High (Fix This Sprint)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in touched files | - | - | - |

### Medium (Backlog)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| Daemon channel/webchat orchestration is still concentrated in several multi-hundred-line methods | `runtime/src/gateway/daemon.ts` (`wireTelegram`, `wireExternalChannel`, `executeWebChatConversationTurn`) | Stateful-session helpers are isolated now, but incident work still has to cut through channel wiring, webchat trace plumbing, and turn execution in one file | Extract webchat turn orchestration and shared channel execution plumbing into dedicated gateway modules before the next autonomy-hardening pass |
| Chat executor transport/tool loop remains a large mixed-responsibility surface | `runtime/src/llm/chat-executor.ts` (`executePlannerPath`, `executeToolCallLoop`, `callWithFallback`) | Planner salvage and verifier rounds are extracted, but provider fallback, tool execution, and evaluation still make continuity changes expensive | Extract provider transport/fallback and tool-loop policy into dedicated helper modules next |

### Duplications Found
| Pattern | Locations | Lines | Refactor To |
|---------|-----------|-------|-------------|
| Resolved: web-session runtime-state persistence, session stateful option shaping, and continuation-anchor persistence are no longer embedded inside the daemon | `runtime/src/gateway/daemon.ts` | ~210 lines moved | `runtime/src/gateway/daemon-session-state.ts` |

### Summary
- Total issues: 2 medium
- Estimated cleanup: 2 large files
- Recommended priority: extract daemon webchat/channel execution, then peel provider transport out of `ChatExecutor`

---

## Runtime Daemon Text-Channel Turn Decomposition - 2026-03-09

### Scope
- Focused post-refactor scan for the daemon channel execution surfaces changed in this pass:
  - `runtime/src/gateway/daemon.ts`
  - `runtime/src/gateway/daemon-text-channel-turn.ts`
  - `runtime/src/gateway/daemon-text-channel-turn.test.ts`
  - `runtime/src/llm/chat-executor.ts`

### Critical (Fix Now)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in touched files | - | - | - |

### High (Fix This Sprint)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in touched files | - | - | - |

### Medium (Backlog)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| Webchat conversation execution is still a large daemon-owned orchestration path | `runtime/src/gateway/daemon.ts` (`executeWebChatConversationTurn`) | Text channels now share one execution helper, but the highest-volume webchat turn path still mixes trace, session continuity, memory, lifecycle signaling, and transport in one method | Extract the webchat conversation turn into its own gateway module next |
| Chat executor transport/tool loop remains the next large continuity bottleneck | `runtime/src/llm/chat-executor.ts` (`executePlannerPath`, `executeToolCallLoop`, `callWithFallback`) | Daemon channel duplication is reduced, but autonomy work still needs to cut through a large executor transport/tool path | Extract provider fallback/transport or tool-loop policy into dedicated helper modules after the webchat turn split |

### Duplications Found
| Pattern | Locations | Lines | Refactor To |
|---------|-----------|-------|-------------|
| Resolved: Telegram and generic external channels no longer carry separate copies of the traced chat-execution/session-persistence path | `runtime/src/gateway/daemon.ts` | ~320 lines moved | `runtime/src/gateway/daemon-text-channel-turn.ts` |

### Summary
- Total issues: 2 medium
- Estimated cleanup: 2 large files
- Recommended priority: extract `executeWebChatConversationTurn`, then peel provider transport/tool-loop policy out of `ChatExecutor`

---

## Runtime WebChat Turn Decomposition - 2026-03-09

### Scope
- Focused post-refactor scan for the daemon webchat execution surfaces changed in this pass:
  - `runtime/src/gateway/daemon.ts`
  - `runtime/src/gateway/daemon-webchat-turn.ts`
  - `runtime/src/gateway/daemon-webchat-turn.test.ts`
  - `runtime/src/gateway/daemon-llm-failure.ts`
  - `runtime/src/llm/chat-executor.ts`

### Critical (Fix Now)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in touched files | - | - | - |

### High (Fix This Sprint)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in touched files | - | - | - |

### Medium (Backlog)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| Daemon background-run/autonomy control is still concentrated in a large runtime surface | `runtime/src/gateway/daemon.ts` (`wireAutonomousFeatures`, `handleWebChatInboundMessage`, `executeWebChatDoomStopTurn`) | Channel execution is cleaner now, but autonomy routing, background-run control, and Doom special-casing still live together inside the daemon | Extract the background-run/control path or the inbound webchat decision layer into dedicated gateway modules next |
| Chat executor transport/tool loop remains the main non-daemon continuity hotspot | `runtime/src/llm/chat-executor.ts` (`executePlannerPath`, `executeToolCallLoop`, `callWithFallback`) | Daemon execution paths are slimmer, but provider transport, tool execution, and evaluator flow still share one large executor file | Extract provider fallback/transport or tool-loop policy into helper modules next |

### Duplications Found
| Pattern | Locations | Lines | Refactor To |
|---------|-----------|-------|-------------|
| Resolved: the full webchat conversation turn no longer sits inline inside the daemon | `runtime/src/gateway/daemon.ts` | ~430 lines moved | `runtime/src/gateway/daemon-webchat-turn.ts` |
| Resolved: LLM surface-failure formatting is no longer daemon-local when reused by extracted runtime helpers | `runtime/src/gateway/daemon.ts` | ~20 lines moved | `runtime/src/gateway/daemon-llm-failure.ts` |

### Summary
- Total issues: 2 medium
- Estimated cleanup: 2 large files
- Recommended priority: extract daemon background-run/inbound control, then peel provider transport/tool-loop policy out of `ChatExecutor`
