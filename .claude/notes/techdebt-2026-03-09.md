## Tech Debt Report - 2026-03-09

### Scope
- Focused end-of-session scan for the Live TUI surfaces touched in this pass:
  - `scripts/agenc-watch.mjs`
  - `scripts/agenc-trace-watch.mjs`
- Detailed watch/TUI findings remain in `.claude/notes/techdebt-2026-03-09-agenc-watch-tui.md`

### Critical (Fix Now)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in touched files | - | - | - |

### High (Fix This Sprint)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in touched files | - | - | - |

### Medium (Backlog)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| Live and trace panes still duplicate terminal-theme primitives | `scripts/agenc-watch.mjs`, `scripts/agenc-trace-watch.mjs` | Aesthetic changes can drift again because palette, badge, and frame logic are edited in two places | Extract a shared `terminal-theme` helper before the next major TUI pass |
| Live watch remains a large combined transport/state/render script | `scripts/agenc-watch.mjs` | Future visual or behavioral changes still carry high regression risk | Split render/layout from websocket/event/input plumbing |

### Duplications Found
| Pattern | Locations | Lines | Refactor To |
|---------|-----------|-------|-------------|
| Terminal palette and chrome helpers repeated across live and trace watchers | `scripts/agenc-watch.mjs`, `scripts/agenc-trace-watch.mjs` | ~35 | Shared terminal theme module |

### Summary
- Total issues: 2
- Estimated cleanup: 2 files
- Recommended priority: extract shared terminal-theme primitives, then split the live watch transport/render boundary

---

## Runtime Addendum - 2026-03-09

### Scope
- Focused end-of-session scan for the runtime continuity and compaction surfaces touched in this pass:
  - `runtime/src/gateway/daemon.ts`
  - `runtime/src/gateway/delegation-tool.ts`
  - `runtime/src/gateway/sub-agent.ts`
  - `runtime/src/gateway/tool-handler-factory.ts`
  - `runtime/src/llm/chat-executor.ts`
  - `runtime/src/llm/chat-executor-text.ts`
  - `runtime/src/llm/grok/adapter.ts`
  - `runtime/src/memory/ingestion.ts`

### Critical (Fix Now)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in touched files | - | - | - |

### High (Fix This Sprint)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in touched files | - | - | - |

### Medium (Backlog)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| Core continuity path still depends on very large orchestration files | `runtime/src/gateway/daemon.ts`, `runtime/src/llm/chat-executor.ts`, `runtime/src/llm/grok/adapter.ts`, `runtime/src/llm/chat-executor-text.ts` | Stateful replay and memory regressions remain expensive to reason about and easy to re-break because flow control, transport, and policy logic are concentrated in a few hotspots | Continue decomposition into smaller continuity/transport/memory modules before the next major autonomy pass |
| Compaction ingestion policy is still encoded inline with broader memory ingestion behavior | `runtime/src/memory/ingestion.ts` | Future hook changes can reintroduce false-positive warnings or mixed semantics between generated and non-generated summaries | Extract explicit compact-event validation helpers and cover manual/local compaction payload shapes directly |

### Duplications Found
| Pattern | Locations | Lines | Refactor To |
|---------|-----------|-------|-------------|
| Stateful continuation and routing logic still spans daemon, executor, and adapter layers with repeated diagnostics shaping | `runtime/src/gateway/daemon.ts`, `runtime/src/llm/chat-executor.ts`, `runtime/src/llm/grok/adapter.ts` | ~60-90 per layer | Shared continuity diagnostics and request-shaping helpers |

### Summary
- Total issues: 2 medium
- Estimated cleanup: 4 files
- Recommended priority: continue decomposing the continuity path, then isolate compaction hook validation

---

## Runtime Endurance Addendum - 2026-03-09

### Scope
- Focused scan for the additional endurance-memory stabilization work validated from `~/.agenc/daemon.log` in replay runs `R10` and `R11`:
  - `runtime/src/channels/webchat/plugin.ts`
  - `runtime/src/gateway/sub-agent.ts`
  - `runtime/src/gateway/tool-handler-factory.ts`
  - `runtime/src/llm/chat-executor.ts`
  - `runtime/src/llm/chat-executor-planner.ts`
  - `runtime/src/llm/chat-executor-text.ts`
  - `scripts/agenc-watch.mjs`

### Critical (Fix Now)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in touched files | - | - | - |

### High (Fix This Sprint)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in touched files | - | - | - |

### Medium (Backlog)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| Planner output contract is still provider-fragile even after salvage support | `runtime/src/llm/chat-executor-planner.ts`, `runtime/src/llm/chat-executor.ts` | The runtime now recovers from direct planner tool calls, but future planner drift can still force more compatibility code into the executor path | Isolate planner-response normalization behind a dedicated adapter and expand replay fixtures for malformed planner output shapes |
| Delegated exact-response enforcement is split across session reuse, prompt sanitization, and text reconciliation layers | `runtime/src/gateway/tool-handler-factory.ts`, `runtime/src/llm/chat-executor-text.ts`, `runtime/src/gateway/sub-agent.ts` | Parent/child memory endurance remains correct, but future changes can re-break store vs recall handling or literal restoration in subtle ways | Consolidate delegated exact-response contract handling into a shared helper with store/recall classification tests |

### Duplications Found
| Pattern | Locations | Lines | Refactor To |
|---------|-----------|-------|-------------|
| Delegated-memory contract shaping is repeated across delegation and text-reconciliation layers | `runtime/src/gateway/tool-handler-factory.ts`, `runtime/src/llm/chat-executor-text.ts` | ~40-60 | Shared exact-contract/delegation normalization helper |

### Summary
- Total issues: 2 medium
- Estimated cleanup: 5 files
- Recommended priority: isolate planner-response normalization, then consolidate delegated exact-response handling

---

## Runtime Child-Memory Addendum - 2026-03-09

### Scope
- Focused scan for the delegated child-memory contract fixes validated from `~/.agenc/daemon.log` during live replay `MEMCORE-20260309-1302`:
  - `runtime/src/gateway/tool-handler-factory.ts`
  - `runtime/src/utils/delegation-validation.ts`
  - `runtime/src/llm/chat-executor-planner.ts`
  - `runtime/src/llm/chat-executor.test.ts`

### Critical (Fix Now)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in touched files | - | - | - |

### High (Fix This Sprint)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None found in touched files | - | - | - |

### Medium (Backlog)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| Planner salvage is still the normal path for delegated memory turns instead of strict planner JSON | `runtime/src/llm/chat-executor-planner.ts`, `runtime/src/llm/chat-executor.ts` | Memory continuity now works, but planner-shape drift is still masked by compatibility logic and can hide future regressions behind `planner_tool_call_salvaged` noise | Add a dedicated planner-output normalization layer and explicit replay fixtures for function-call-first planner responses |
| Delegated contract normalization remains split between tool dispatch and validation | `runtime/src/gateway/tool-handler-factory.ts`, `runtime/src/utils/delegation-validation.ts` | Store/recall fixes are correct today, but future changes can drift literal rewriting, placeholder matching, and acceptance enforcement back out of sync | Extract a shared delegated-contract normalization module used by both dispatch and validation |

### Duplications Found
| Pattern | Locations | Lines | Refactor To |
|---------|-----------|-------|-------------|
| Delegated exact-output contract shaping is duplicated between tool dispatch rewriting and validator-side placeholder matching | `runtime/src/gateway/tool-handler-factory.ts`, `runtime/src/utils/delegation-validation.ts` | ~45-70 | Shared delegated-contract normalization helper |

### Summary
- Total issues: 2 medium
- Estimated cleanup: 4 files
- Recommended priority: isolate planner normalization, then unify delegated contract shaping across dispatch and validation
